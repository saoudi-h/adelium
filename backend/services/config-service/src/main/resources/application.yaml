server:
  port: 8888

spring:
  application:
    name: config-service
  config:
    import: application-secret.yaml
  cloud:
    config:
      enabled: false
      server:
        git:
          uri: "https://github.com/saoudi-h/adelium-config-cloud-github"
          username:
          password:
management:
  endpoints:
    web:
      exposure:
        include: '*'